version: 2

models:
  - name: cost_joins
    description: "A starter dbt model"
    columns: 
      - name: Year
        column_type: integer
        description: "The primary key for this table"
        data_tests:
          - not_null